import { ComingSoon } from "@/client/shell/coming-soon";

export default function ExplorarPage() {
  return (
    <ComingSoon
      title="Explorar"
      description="Descoberta de eventos próximos, filtros e 'Quero ir' chegam em breve — a API já existe."
    />
  );
}
